# Legacy JavaScript (Non-Runtime)

Files in this folder are preserved historical copies and are **not** loaded by the app at runtime.

The app serves static assets from `public/`, so active frontend scripts live under `public/js/`.
